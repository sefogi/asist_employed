# URL base del API. En desarrollo apunta al backend local (pnpm dev en backend/).
# En producción (detrás de nginx) se sirve same-origin y basta con /api/v1.
VITE_API_URL=http://localhost:3000/api/v1
